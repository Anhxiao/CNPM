const Header = ({ title }) => {

    return (

        <header className="page-header">

            <h2>

                {title}

            </h2>

        </header>

    );

};

export default Header;